# دليل نظام توزيع الغاز 🔥

## نظرة عامة
نظام توزيع الغاز يوفر خدمة طلب اسطوانات الغاز مع التوصيل المباشر إلى موقع العميل. النظام يعمل بنفس منهجية التاكسي مع البحث التدريجي عن أقرب موزع متاح.

## 🚀 المميزات الرئيسية

### للعملاء:
- طلب اسطوانات الغاز بأنواع مختلفة (12كج، 25كج، صغيرة)
- البحث التلقائي عن أقرب الموزعين
- تتبع الطلب في الوقت الفعلي
- نظام تقييم الموزعين
- دفع نقدي عند التسليم

### للموزعين:
- تسجيل بسيط مع تحديد نطاق التغطية
- استلام إشعارات الطلبات تلقائياً
- إدارة المخزون والأسعار
- تتبع الأرباح والإحصائيات

---

## 📊 هيكل قاعدة البيانات

### 1. جدول الموزعين (gas_distributors)
```sql
- id: معرف فريد
- driver_id: مرتبط بجدول المستخدمين
- business_name: اسم المؤسسة
- license_plate: رقم لوحة المركبة
- phone: رقم الهاتف
- current_latitude/longitude: الموقع الحالي
- coverage_radius: نطاق التغطية (كم)
- cylinder_*_price: أسعار الاسطوانات المختلفة
- is_available/verified/online: حالة الموزع
- rating: تقييم الموزع
```

### 2. جدول الطلبات (gas_delivery_orders)
```sql
- id: معرف فريد للطلب
- order_number: رقم الطلب
- customer_id: معرف العميل
- distributor_id: معرف الموزع المقبول
- cylinder_type: نوع الاسطوانة
- quantity: الكمية
- total_amount: المبلغ الإجمالي
- delivery_address: عنوان التسليم
- status: حالة الطلب
- search_radius: نطاق البحث الحالي
```

### 3. جدول الإشعارات (gas_order_notifications)
```sql
- order_id: معرف الطلب
- distributor_id: معرف الموزع
- distance_km: المسافة
- sent_at: وقت الإرسال
- response: الاستجابة (accept/reject/timeout)
```

---

## 🔄 خوارزمية البحث التدريجي

### المرحلة 1: البحث الأولي (5 كم)
```sql
1. العميل ينقر على "اطلب غاز"
2. النظام يبحث عن موزعين في دائرة نصف قطرها 5 كم
3. إرسال إشعارات للموزعين المتاحين
4. انتظار الاستجابة لمدة 60 ثانية
```

### المرحلة 2: توسيع النطاق (+5 كم)
```sql
1. إذا لم يستجب أحد، توسيع البحث إلى 10 كم
2. إرسال إشعارات للموزعين الجدد
3. انتظار الاستجابة لمدة 60 ثانية
```

### المرحلة 3: البحث النهائي (حتى 50 كم)
```sql
1. الاستمرار في التوسيع حتى الوصول للحد الأقصى
2. إذا لم يجد موزعين، تحديث حالة الطلب إلى "no_distributors_found"
```

---

## 📱 واجهات المستخدم

### 1. صفحة طلب الغاز (/gas-delivery/index.tsx)
- اختيار نوع الاسطوانة ✅
- تحديد الكمية ✅
- عرض الموزعين القريبين ✅
- زر إنشاء الطلب ✅

### 2. صفحة تتبع الطلب (/gas-delivery/order-tracking.tsx)
- عرض حالة الطلب الحالية ✅
- تفاصيل الطلب والموزع ✅
- زر الاتصال بالموزع ✅
- إمكانية إلغاء الطلب ✅

### 3. صفحة تسجيل الموزع (/auth/gas-distributor-register.tsx)
- معلومات المؤسسة والمركبة ✅
- تحديد الموقع تلقائياً ✅
- تحديد نطاق التغطية ✅
- تحديد أسعار الاسطوانات ✅

### 4. مكون قائمة الطلبات (components/gas-delivery/GasOrdersList.tsx)
- عرض جميع طلبات العميل ✅
- تصفية حسب الحالة ✅
- إمكانية تتبع كل طلب ✅

---

## 🔧 الوظائف المتقدمة

### 1. دالة البحث عن الموزعين
```sql
find_available_gas_distributors(
  delivery_lat, delivery_lng, 
  cylinder_type, search_radius, 
  excluded_distributors
)
```

### 2. دالة الإشعارات التدريجية
```sql
notify_gas_distributors_progressive(order_uuid)
```

### 3. دالة تحديث الموقع
```sql
update_gas_distributor_location(
  distributor_uuid, lat, lng, 
  heading, speed, accuracy
)
```

### 4. دالة حساب المسافة
```sql
calculate_distance_km(lat1, lng1, lat2, lng2)
```

---

## 🔐 سياسات الأمان (RLS)

### للموزعين:
- يمكن للموزعين رؤية بياناتهم فقط
- يمكن للعملاء رؤية الموزعين المتاحين والمعتمدين

### للطلبات:
- العملاء يرون طلباتهم فقط
- الموزعون يرون الطلبات المرسلة إليهم
- يمكن تحديث الطلبات من قبل العميل أو الموزع المقبول

### للإشعارات:
- الموزعون يرون إشعاراتهم فقط
- النظام يدير الإشعارات تلقائياً

---

## 🎯 حالات الطلب

| الحالة | الوصف | الإجراءات المتاحة |
|--------|-------|------------------|
| `searching` | جاري البحث عن موزع | إلغاء الطلب، إعادة البحث |
| `accepted` | تم قبول الطلب | إلغاء الطلب، الاتصال بالموزع |
| `en_route` | الموزع في الطريق | الاتصال بالموزع |
| `arrived` | وصل الموزع | الاتصال بالموزع |
| `delivering` | جاري التسليم | - |
| `completed` | تم التسليم | تقييم الخدمة، طلب مرة أخرى |
| `cancelled` | تم الإلغاء | طلب مرة أخرى |
| `no_distributors_found` | لا يوجد موزعين | المحاولة مرة أخرى |

---

## 📋 خطوات التشغيل

### 1. إنشاء قاعدة البيانات
```bash
# تشغيل ملف SQL في Supabase
psql -f scripts/create-gas-delivery-tables.sql
```

### 2. تحديث التطبيق
- إضافة خدمة توزيع الغاز في الصفحة الرئيسية ✅
- إضافة تبويب في قسم الرحلات ✅
- تحديث نظام التوجيه ✅

### 3. اختبار النظام
- تسجيل موزع جديد
- إنشاء طلب من العميل
- تتبع عملية البحث والقبول
- تأكيد التسليم والتقييم

---

## 🚀 التطويرات المستقبلية

### المرحلة القادمة:
- [ ] تطبيق منفصل للموزعين
- [ ] نظام الدفع الإلكتروني
- [ ] تتبع GPS في الوقت الفعلي
- [ ] نظام التقييمات المتقدم
- [ ] إشعارات push للموزعين
- [ ] تقارير الأرباح والمبيعات

### تحسينات أخرى:
- [ ] دعم اللغة الإنجليزية
- [ ] نظام العروض والخصومات
- [ ] برنامج الولاء للعملاء
- [ ] API للتكامل مع أنظمة أخرى

---

## 📞 الدعم والصيانة

### ملفات مهمة للمراقبة:
- `scripts/create-gas-delivery-tables.sql` - هيكل قاعدة البيانات
- `app/gas-delivery/index.tsx` - واجهة طلب الغاز
- `components/gas-delivery/GasOrdersList.tsx` - إدارة الطلبات
- `utils/supabase.ts` - إعدادات قاعدة البيانات

### لوحة المراقبة:
```sql
-- إحصائيات الطلبات اليومية
SELECT status, COUNT(*) as count 
FROM gas_delivery_orders 
WHERE DATE(created_at) = CURRENT_DATE 
GROUP BY status;

-- أداء الموزعين
SELECT business_name, total_orders, rating 
FROM gas_distributors 
WHERE is_verified = true 
ORDER BY rating DESC;
```

---

## ✅ الملخص النهائي

تم إنشاء نظام توزيع الغاز بنجاح بالمميزات التالية:

🔥 **قاعدة البيانات**: جداول شاملة مع وظائف ذكية للبحث الجغرافي
📱 **واجهات المستخدم**: صفحات سهلة الاستخدام للطلب والتتبع والتسجيل  
🔄 **البحث التدريجي**: خوارزمية ذكية للعثور على أقرب موزع
🔐 **الأمان**: سياسات RLS شاملة لحماية البيانات
📊 **التتبع**: نظام متكامل لتتبع الطلبات والمواقع

النظام جاهز للاستخدام ويمكن توسيعه مستقبلاً بمميزات إضافية حسب الحاجة! 🚀
