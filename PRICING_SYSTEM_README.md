# 🚗 نظام التسعير الذكي لتطبيق IGTaxi

## 📊 نظرة عامة على النظام

تم تطوير نظام تسعير متطور وجذاب لتطبيق سيارات الأجرة يقدم أسعاراً تنافسية للعملاء وأرباحاً عادلة للسائقين. النظام يعتمد على نوع الوقود المستخدم في المركبة ويشجع على استخدام المركبات الصديقة للبيئة.

## 🎯 المزايا الرئيسية

### للعملاء:
- ✅ أسعار أقل من التطبيقات المنافسة
- ✅ تسعير شفاف ومفصل
- ✅ تشجيع استخدام المركبات النظيفة
- ✅ رسوم عادلة للانتظار في الزحمة

### للسائقين:
- ✅ نصيب عادل 90% من إجمالي التكلفة
- ✅ دخل ثابت حتى في الرحلات القصيرة
- ✅ مكافآت لاستخدام المركبات البيئية
- ✅ رسوم إضافية في أوقات الذروة

### للتطبيق:
- ✅ عمولة معقولة 10% من كل رحلة
- ✅ نظام مراجعة شامل للإيرادات
- ✅ تتبع مفصل لأداء السائقين
- ✅ إحصائيات دقيقة للعمليات

## 💰 هيكل التسعير

### الرسوم الأساسية:
```javascript
الرسوم الثابتة:
├── رسوم البداية: 0.35 دينار
├── الحد الأدنى للرحلة: 0.90 دينار
└── رسوم الانتظار: 0.03 دينار/دقيقة
```

### التسعير حسب نوع الوقود:
```javascript
أسعار الوقود (دينار/كم):
├── ⛽ بنزين: 0.17
├── 🚗 ديزل: 0.16
├── 🔋 هجين: 0.15
└── ⚡ كهرباء: 0.14
```

### تقسيم الإيرادات:
```javascript
توزيع الأرباح:
├── السائق: 90%
└── التطبيق: 10%
```

## 🧮 أمثلة على الحسابات

### مثال 1: رحلة قصيرة (3 كم - بنزين)
```
الحساب: 0.35 + (3 × 0.17) = 0.86 دينار
بعد الحد الأدنى: 0.90 دينار ← تطبيق الحد الأدنى
نصيب السائق: 0.81 دينار (90%)
عمولة التطبيق: 0.09 دينار (10%)
```

### مثال 2: رحلة طويلة (75 كم - كهرباء)
```
الحساب: 0.35 + (75 × 0.14) = 10.85 دينار
نصيب السائق: 9.77 دينار (90%)
عمولة التطبيق: 1.08 دينار (10%)
توفير مقارنة بالبنزين: 2.25 دينار
```

### مثال 3: رحلة مع انتظار (10 كم - هجين + 15 دقيقة انتظار)
```
تكلفة المسافة: 0.35 + (10 × 0.15) = 1.85 دينار
رسوم الانتظار: 15 × 0.03 = 0.45 دينار
الإجمالي: 2.30 دينار
نصيب السائق: 2.07 دينار (90%)
عمولة التطبيق: 0.23 دينار (10%)
```

## 🔧 التقنيات المستخدمة

### Frontend (React Native):
- TypeScript للحماية من الأخطاء
- Custom Hooks لإدارة التسعير
- مكونات تفاعلية لعرض التكلفة
- واجهة مستخدم عربية وإنجليزية

### Backend (Supabase PostgreSQL):
- دوال PostgreSQL للحسابات السريعة
- مُحفِزات تلقائية لتحديث الإيرادات
- فهارس محسّنة لأداء سريع
- أمان على مستوى الصفوف (RLS)

### قاعدة البيانات:
- جداول منفصلة للإيرادات اليومية والشهرية
- تتبع مفصل لكل رحلة وتكلفتها
- إحصائيات شاملة للسائقين
- تقارير إدارية متقدمة

## 📁 هيكل الملفات

```
utils/
├── fareCalculator.ts          # دالة حساب التكلفة الرئيسية
└── storage.ts                 # إدارة التخزين

hooks/
├── useFareCalculator.ts       # Hook للتسعير
└── useAuth.ts                 # Hook للمصادقة

components/
├── fare/
│   └── FareComponents.tsx     # مكونات عرض التكلفة
└── maps/
    └── MapView.tsx            # عرض الخريطة

app/
├── trips/
│   └── [id].tsx              # صفحة تفاصيل الرحلة
└── auth/
    └── driver-register.tsx    # تسجيل السائق

supabase/
└── migrations/
    └── 20250119_008_create_pricing_system.sql  # إعداد قاعدة البيانات
```

## 🚀 كيفية الاستخدام

### 1. حساب تكلفة رحلة:
```typescript
import { useFareCalculator } from '@/hooks/useFareCalculator';

const { calculateTripFare } = useFareCalculator();

const fareResult = calculateTripFare({
  distance_km: 10,
  fuel_type: 'hybrid',
  waiting_time_minutes: 5,
  surge_multiplier: 1.2
});

console.log(`التكلفة الإجمالية: ${fareResult.total_fare} دينار`);
console.log(`أرباح السائق: ${fareResult.driver_earnings} دينار`);
```

### 2. عرض تفاصيل التكلفة:
```typescript
import { FareDisplay } from '@/components/fare/FareComponents';

<FareDisplay
  distance_km={15}
  fuel_type="electric"
  waiting_time_minutes={10}
  showDriverBreakdown={true}
/>
```

### 3. إحصائيات السائق:
```typescript
import { useDriverEarnings } from '@/hooks/useFareCalculator';

const { dailyEarnings, monthlyEarnings } = useDriverEarnings(driverId);
```

## 📈 قاعدة البيانات

### الجداول الرئيسية:

#### 1. جدول إعدادات التسعير (`pricing_config`)
```sql
- config_name: نوع الإعداد
- base_fare: رسوم البداية
- minimum_fare: الحد الأدنى
- fuel_rates: أسعار الوقود (JSON)
- app_commission_rate: نسبة عمولة التطبيق
```

#### 2. جدول الرحلات المحدث (`trips`)
```sql
- fuel_type: نوع الوقود
- total_customer_fare: المبلغ الذي يدفعه العميل
- driver_earnings: أرباح السائق (90%)
- app_commission: عمولة التطبيق (10%)
- waiting_time_minutes: وقت الانتظار
- fare_breakdown: تفصيل التكلفة (JSON)
```

#### 3. جدول الإيرادات اليومية (`driver_daily_earnings`)
```sql
- driver_id: معرف السائق
- earning_date: تاريخ الإيرادات
- total_trips: عدد الرحلات
- total_revenue: إجمالي الإيرادات
- driver_earnings: أرباح السائق
- app_commission: عمولة التطبيق
```

#### 4. جدول الإيرادات الشهرية (`driver_monthly_earnings`)
```sql
- driver_id: معرف السائق
- year, month: السنة والشهر
- total_trips: إجمالي الرحلات
- working_days: أيام العمل
- best_day_earnings: أفضل يوم
```

## 🔒 الأمان

### حماية على مستوى الصفوف (RLS):
- السائقون يرون إيراداتهم فقط
- المديرون يرون جميع البيانات
- العملاء يرون رحلاتهم فقط

### التحقق من البيانات:
- فحص صحة نوع الوقود
- التأكد من المسافات والأوقات
- منع التلاعب في الأسعار

## 🧪 الاختبارات

### تشغيل الاختبارات:
```bash
cd "مجلد المشروع"
node test-pricing.js
```

### نتائج الاختبارات:
```
✅ رحلة 3 كم بنزين: 0.90 دينار (تطبيق الحد الأدنى)
✅ رحلة 75 كم كهرباء: 10.85 دينار (توفير 3.45 دينار)
✅ رحلة 75 كم ديزل: 12.35 دينار (توفير 1.95 دينار)
✅ رحلة مع انتظار: حساب دقيق لرسوم الانتظار
```

## 📊 المقارنة مع المنافسين

| نوع الرحلة | التطبيقات الأخرى | IGTaxi | التوفير |
|------------|------------------|--------|---------|
| 75 كم بنزين | 14.30 دينار | 13.10 دينار | 1.20 دينار |
| 75 كم كهرباء | 14.30 دينار | 10.85 دينار | 3.45 دينار |
| رحلة قصيرة | متغير | 0.90 دينار | ثابت |

## 🌱 التأثير البيئي

### تشجيع المركبات النظيفة:
- خصم 3 فلس/كم للمركبات الكهربائية
- خصم 2 فلس/كم للمركبات الهجينة
- خصم 1 فلس/كم لمركبات الديزل

### النتائج المتوقعة:
- تقليل الانبعاثات بنسبة 15%
- زيادة استخدام المركبات النظيفة
- توفير تكاليف الوقود للسائقين

## 🎯 الخطوات التالية

### المرحلة القادمة:
1. تطبيق نظام التسعير الديناميكي
2. إضافة خصومات للعملاء المنتظمين
3. نظام نقاط الولاء للسائقين
4. تقارير متقدمة للإدارة

### التطوير المستمر:
- مراقبة أداء النظام
- تحليل رضا العملاء والسائقين
- تحسين الخوارزميات
- إضافة مزايا جديدة

## 📞 الدعم والمساعدة

للاستفسارات والدعم التقني:
- 📧 البريد الإلكتروني: support@igtaxi.com
- 📱 الهاتف: +962-XXX-XXXX
- 🌐 الموقع: www.igtaxi.com

---

**تم تطوير هذا النظام بعناية فائقة لضمان العدالة والشفافية لجميع المستخدمين** 🚗💙
